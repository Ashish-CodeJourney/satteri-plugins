---
"satteri-katex": minor
---

Add `satteri-katex`, a port of `rehype-katex` that renders Sätteri's parsed math with KaTeX.
Failing expressions become an escaped `katex-error` span instead of breaking the build.
