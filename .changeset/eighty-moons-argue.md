---
"satteri-autolink-headings": minor
---

Add `satteri-autolink-headings`, a port of `rehype-autolink-headings` supporting all five behaviors
plus `content`, `properties`, `headingProperties`, `test` and `group`.
