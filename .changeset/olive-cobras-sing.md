---
"satteri-slug": patch
"satteri-autolink-headings": patch
"satteri-katex": patch
---

First release published through GitHub Actions with npm trusted publishing, so these tarballs carry
a signed provenance attestation. `0.1.0` was published from a terminal to bootstrap the trusted
publisher and has no attestation. No code changes.
