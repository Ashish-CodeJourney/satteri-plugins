# satteri-breaks

## 0.1.0

### Minor Changes

- 174981e: Add `satteri-breaks`, a port of `remark-breaks` that turns single newlines into `<br>`. Fenced and
  inline code are untouched, and CRLF line endings are handled.
